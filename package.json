{
  "name": "gridpacking",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "main": "gridpacking.js",
  "scripts": {
    "test": "vitest run --globals",
    "test:watch": "vitest --ui --open --globals",
    "start": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "@vitest/ui": "^1.6.0",
    "vite": "^5.2.13",
    "vitest": "^1.6.0"
  }
}
